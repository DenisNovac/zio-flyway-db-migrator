include "params.conf"


url = "jdbc:postgresql://localhost:5432/main"
url = ${?DB_URL}

user = "postgres"
user = ${?DB_USER}

password = "postgres"
password = ${?DB_PASSWORD}

migrationsLocation = "classpath:flyway"
